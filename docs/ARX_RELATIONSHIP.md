# ARX relationship

HHS is a separate specialized project. The owner's framing is ARX for general machine-reality inspection, HHS for Hugging Face requirements → local compatibility → structured plans. Local ARX documentation already describes project-aware compatibility concepts; this is useful overlap, not a reason to merge identities or duplicate its implementation.

## Bounded read-only inspection

Read known documents in `C:\Codex-Projects\ARX`: README.md, docs/architecture.md and docs/gpu-compute-intelligence.md, plus applicable repository/document instructions. Only top-level entries of that known root, its docs directory and `C:\AI-Workspaces\ARX` were listed; the latter contained batch directories that were not explored. No ARX commands, tests or application were run. No ARX source was copied or modified.

The README identifies itself as ARX 4.0.0 Beta 6. This is OBSERVED documentation, not a reverified release/build claim. Public architectural reference: [ARX](https://github.com/chatgptopenaiagi/ARX).

## Reusable ideas

| ARX idea observed in documentation | HHS adaptation proposed |
|---|---|
| Bounded static inspection and fixed diagnostic probes | Metadata/file budgets and trusted local provider probes |
| Canonical evidence with thin output surfaces | One evidence-backed result projected into human/agent manifests |
| Provider graph and execution-context resolution | Keep multiple Python/toolkit/framework providers distinct; choose a target explicitly |
| Availability, resolution, compatibility, relevance and remediation separated | Avoid machine-wide yes/no and unjustified installation advice |
| Layered GPU facts | Separate hardware, driver capability, toolkit, native libraries, framework build and workload resource fit |
| Redacted optional advisory boundary | Vendor-neutral opt-in exports; advice never promoted directly into evidence |
| Foreign-path semantics | Preserve Windows/WSL/Linux path identity without accidentally resolving on the wrong OS |

## Do not copy semantics blindly

ARX documentation gives VERIFIED a semantic-validation meaning in some contexts and distinguishes serialized provenance. HHS's initial definition is specifically an important observation independently confirmed by another trusted probe or consistency check. Record an explicit mapping before any future evidence import. Schema validity does not verify hardware health or workload compatibility.

HHS adds repository revision/type, model variants/weights/quantization, Hugging Face-specific requirements, portable intent-aware plans and agent-neutral export as its specialist focus. No runtime dependency on ARX is selected today. Future source reuse requires an explicit scope and licensing review; today reuses only independently described architectural ideas.
